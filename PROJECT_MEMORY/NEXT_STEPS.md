# Next Steps

1. User will drop background images and service card photos into `public/images/` according to `public/images/README.md`.
2. As soon as the user adds their images, the site will automatically render them through `<SafeImage>` with zero code changes required.
3. Deploy to Vercel or production hosting:
   - Run `vercel` or link GitHub repository to Vercel.
   - 100% static output requires zero server configuration.
